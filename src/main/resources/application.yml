spring:
  application:
    name: Blogging-app

  datasource:
    url: jdbc:postgresql://p-rapid-mud-aprebjlu-pooler.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_XyPR6AT3ueWa
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  cloud:
    discovery:
          enabled: false

project:
  image: images/

logging:
  level:
    org:
      springframework:
        security: DEBUG

eureka:
  client:
    enabled: false
